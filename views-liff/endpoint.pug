doctype html
html
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1, viewport-fit=cover, user-scalable=no")
    title 17 來玩板
  body
    script(crossorigin="anonymous", src="https://static.line-scdn.net/liff/edge/2/sdk.js")
    script.
      (async () => {
        console.log('endpoint')
        await liff.init({ liffId: '#{LIFF_FULL}' })
        const lp = new URL(location).searchParams.get('lp')
        if (typeof lp !== 'string') return
        location = new URL(`${location.pathname.replace(/\/$/)}/${lp.replace(/^\//)}`, location).href
      })()
